apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "loki.fullname" . }}-grafana-dashboard
  labels:
    grafana_dashboard: "1"
data:
  loki-dashboard.json: |
    { "__inputs": [], "__requires": [], "panels": [], "title": "Loki Dashboard", "version": 1 }
